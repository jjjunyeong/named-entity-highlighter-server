# a python django server to extract named entity from web url

1. create ner/model directory
2. save senna_bi_lstm_crf.tf machine learning model inside ner/model directory
3. run server
- python manage.py runserver